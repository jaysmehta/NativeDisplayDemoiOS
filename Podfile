# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'NativeDisplayDemo' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for NativeDisplayDemo
	pod 'CleverTap-iOS-SDK'
  target 'NativeDisplayDemoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'NativeDisplayDemoUITests' do
    # Pods for testing
  end

end
